# Pi Blue Gradient Editor

A Pi extension that gives the TUI editor a blue gradient border, inline slash-skill completion, and skill-token normalization on submit.

Install the package in a Pi package directory, then load its `./index.ts` extension entrypoint. Run `npm run verify` after installing its development dependencies.
